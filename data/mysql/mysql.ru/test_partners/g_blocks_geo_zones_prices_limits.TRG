TYPE=TRIGGERS
triggers='CREATE DEFINER=`root`@`localhost` TRIGGER `test_partners`.`g_blocks_geo_zones_prices_limits_after_insert_sync_ru` AFTER INSERT ON `test_partners`.`g_blocks_geo_zones_prices_limits`\nFOR EACH ROW BEGIN\n\n\nIF NEW.`geo_zones_id` = 1 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_0 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 2 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_1 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 3 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_2 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 253 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_3 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 94 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_4 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 102 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_5 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 8 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_6 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 5 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_7 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 4 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_8 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 6 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_9 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 7 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_10 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 9 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_11 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 12 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_12 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 11 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_13 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 33 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_14 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 97 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_15 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 113 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_16 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 158 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_17 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 193 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_18 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 105 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_19 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 99 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_20 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 156 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_21 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 178 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_22 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 116 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_23 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 123 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_24 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 14 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_25 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 182 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_26 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 108 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_27 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 150 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_28 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nIF NEW.`geo_zones_id` = 111 THEN\n  UPDATE g_blocks SET minimal_prices_of_click_29 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\nEND IF;\n\nEND' 'CREATE DEFINER=`root`@`localhost` TRIGGER `test_partners`.`g_blocks_geo_zones_prices_limits_after_update_sync_ru` AFTER UPDATE ON `test_partners`.`g_blocks_geo_zones_prices_limits`\nFOR EACH ROW BEGIN\n\nIF OLD.`price` != NEW.`price`\nTHEN\n\n  IF NEW.`geo_zones_id` = 1 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_0 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 2 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_1 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 3 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_2 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 253 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_3 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 94 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_4 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 102 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_5 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 8 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_6 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 5 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_7 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 4 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_8 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 6 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_9 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 7 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_10 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 9 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_11 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 12 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_12 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 11 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_13 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 33 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_14 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 97 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_15 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 113 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_16 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 158 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_17 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 193 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_18 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 105 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_19 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 99 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_20 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 156 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_21 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 178 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_22 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 116 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_23 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 123 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_24 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 14 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_25 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 182 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_26 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 108 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_27 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 150 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_28 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\n  IF NEW.`geo_zones_id` = 111 THEN\n    UPDATE g_blocks SET minimal_prices_of_click_29 = ROUND(NEW.`price` * 1000) WHERE g_blocks.uid = NEW.`g_blocks_uid`;\n  END IF;\n\nEND IF;\n\nEND'
sql_modes=1342177280 1342177280
definers='root@localhost' 'root@localhost'
client_cs_names='utf8' 'utf8'
connection_cl_names='utf8_general_ci' 'utf8_general_ci'
db_cl_names='utf8_general_ci' 'utf8_general_ci'
created=153375877608 153375877609
